branch="$(git rev-parse --abbrev-ref HEAD)"

if [ "$branch" = "main" ]; then
  echo "❌ Direct commits to 'main' are blocked."
  echo "   Create a feature branch instead: git checkout -b my-change"
  exit 1
fi
